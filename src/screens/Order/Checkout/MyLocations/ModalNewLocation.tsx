import { memo } from "react";
import { View, Text } from "react-native";

const ModalNewLocation = memo(() => {
  return (
    <View>
      <Text>ModalNewLocation</Text>
    </View>
  );
});

export default ModalNewLocation;
